<div class="container mt-4">
  <div class="row">
    <div class="col-12">
      <h1 class="mb-4">Minecraft Mobs - Debugging Exercise</h1>

      <div class="alert alert-info" role="alert">
        <strong>Debug URLs:</strong>
        <ul class="mb-0">
          <li><code>?debug_partial=true</code> - Triggers Pry debugging in partial</li>
          <li><%= link_to "Debug Error Page", debug_error_mobs_path, class: "text-decoration-none" %> - Intentional error for debugging</li>
        </ul>
      </div>

      <div class="row">
        <% @mobs.each do |mob| %>
          <div class="col-md-6 col-lg-4 mb-4">
            <div class="card h-100">
              <div class="card-body">
                <h5 class="card-title"><%= mob.name %></h5>
                <p class="card-text"><%= truncate(mob.description, length: 100) %></p>

                <div class="row text-center">
                  <div class="col-4">
                    <small class="text-muted">Health</small>
                    <div class="fw-bold text-success"><%= mob.health %></div>
                  </div>
                  <div class="col-4">
                    <small class="text-muted">Attack</small>
                    <div class="fw-bold text-danger"><%= mob.attack %></div>
                  </div>
                  <div class="col-4">
                    <small class="text-muted">Defense</small>
                    <div class="fw-bold text-primary"><%= mob.defense %></div>
                  </div>
                </div>
              </div>
              <div class="card-footer">
                <%= link_to "View Details", mob_path(mob), class: "btn btn-primary btn-sm" %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

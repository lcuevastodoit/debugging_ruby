<div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-4 animate-fade-in">
  <div class="flex items-center space-x-3">
    <span class="text-2xl">🎉</span>
    <div class="flex-1">
      <h4 class="font-medium text-green-800">Objective Completed!</h4>
      <p class="text-green-600 text-sm">
        <strong><%= objective['title'] %></strong> -
        Earned <%= points %> points using <%= tool %>
      </p>
      <p class="text-xs text-green-500">
        <%= timestamp.strftime("%H:%M:%S") %>
      </p>
    </div>
    <div class="text-green-700 font-bold text-lg">
      +<%= points %> pts
    </div>
  </div>
</div>

<style>
@keyframes fade-in {
  from { opacity: 0; transform: translateY(-10px); }
  to { opacity: 1; transform: translateY(0); }
}

.animate-fade-in {
  animation: fade-in 0.5s ease-out;
}
</style>

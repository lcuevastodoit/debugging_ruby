<div class="bg-indigo-50 border border-indigo-200 rounded-lg p-4 mb-4 animate-bounce-in">
  <div class="flex items-center space-x-3">
    <span class="text-3xl">🎯</span>
    <div class="flex-1">
      <h4 class="font-bold text-indigo-800 mb-1">Milestone Reached!</h4>
      <% if milestone_type == "completion" %>
        <p class="text-indigo-700">
          <strong>Progress Champion!</strong> - Completed <%= completed_count %> objectives!
        </p>
        <div class="mt-2 bg-indigo-100 rounded-full h-2">
          <div class="bg-indigo-500 rounded-full h-2 transition-all duration-500" 
               style="width: <%= (completed_count.to_f / 16 * 100).round(1) %>%"></div>
        </div>
      <% end %>
      <p class="text-xs text-indigo-600 mt-2">
        <%= timestamp.strftime("%H:%M:%S") %>
      </p>
    </div>
    <div class="text-indigo-700 font-bold text-xl">
      ✅<%= completed_count %>
    </div>
  </div>
</div>

<style>
@keyframes bounce-in {
  0% { opacity: 0; transform: scale(0.3); }
  50% { transform: scale(1.05); }
  70% { transform: scale(0.9); }
  100% { opacity: 1; transform: scale(1); }
}

.animate-bounce-in {
  animation: bounce-in 0.6s ease-out;
}
</style>
